version: '3.9'
services:
  web:
    build: .
    container_name: rsvp-aidilfitri
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
